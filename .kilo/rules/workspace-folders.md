# Workspace folders
Inspect <environmental_details> section of the system prompt.
If `Workspace folders` contain list of all VS Code multi-repo workspace.

Consider all these folders as additional folders for your actions.
When searching for dependencies, docs etc...

Consider all of them as standalone git repos with different remotes, 
and use git grep, git ls-files as primarty tools to explore them.

